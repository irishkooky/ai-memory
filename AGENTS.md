# AGENTS.md

このリポジトリは **{{所有者名}}** の外部記憶です。AIエージェントは、ここに書かれた前提・決定事項・過去の直しに従って応答してください。**一度言われたことを二度言わせない**のがこのリポジトリの目的です。

## 読み込み順

1. `profile.md` — 所有者(人・会社)の基本情報。変わりにくい事実
2. `current.md` — 今の優先事項・進行中のこと。冒頭の「最終更新」日付を見て鮮度を判断する
3. `decisions.md` — 決定事項の台帳。「〜にする」「〜はやらない」の一覧。**提案する前に、同じ論点がすでに決まっていないか必ず引く**
4. `feedback.md` — 成果物(文面・資料・コード)への直しから抽出した一般ルール。**文面や成果物を作る前に必ず読む**
5. 相手・取引先が絡む相談 → `people/<相手名>.md`
6. 案件が絡む相談 → `projects/<案件名>.md`

全部読んでも数ページになる量に保つ(下の「記憶は少なく濃く」)。

## 前提

<!-- 所有者が初回セットアップで埋める。例:
- 提案は「月〜木は本業、自社の作業に使えるのは金曜と毎朝90分」という時間制約を前提にすること
- 社外向けの文面は敬体、社内向けは常体
- 数字の根拠が profile.md に無いものは使わない
-->

- 回答は日本語で

## 学習ループ(このリポジトリの心臓部)

**ユーザーが成果物を差し戻した / 自分で書き換えた / 「前も言った」と言った / 新しい決定を口にした**——その時点で、次の手順を**そのセッション内で**やってから終える。「学習しといて」と言われるのを待たない(ほとんどの人は言わない)。

1. **何が起きたかを並べる**: AIの出力(直される側)と、本人の正解(直した後・言った内容)を対にする
2. **一般ルールに言語化する**: この1件の修正ではなく、次に同じ場面で効く形にする(「この文を直す」→「依頼の締めは『〜ください』ではなく『〜いただければと思います』」)。前提知識の不足や、提案の方向性のズレも学びに含める
3. **置き場所を決める**(下の表)
4. **追記する**: 日付と出所を付ける。既存のルールと矛盾するなら古い方を書き換える(併記しない)
5. **コミット・プッシュする**。ここまでで1セット

| 学びの種類 | 書き先 |
|---|---|
| 所有者・会社についての事実、好み、前提 | `profile.md` |
| 決定事項(「〜にする」「〜はやらない」「〜は保留」) | `decisions.md` |
| 文面・資料・成果物への直し(文体、語彙、構成、NG表現) | `feedback.md` |
| 特定の相手にだけ効くこと(この人にはこう書く、この会社の温度感) | `people/<相手名>.md` |
| 特定の案件にだけ効くこと | `projects/<案件名>.md` |
| 作業の手順・型(「〜するときはこうする」) | `.claude/skills/<名前>/SKILL.md` |
| エージェントの振る舞い全般 | この `AGENTS.md` |

- 「**学習しといて**」「**覚えといて**」と言われたら、それはこのループの明示的な実行指示。`.claude/skills/learn` の手順で、セッションを最初から遡って学びを抽出する
- セッション終了時に **Stop hook(`scripts/learn-gate.py`)** が差し戻しの発言や未コミットの変更を検知して終了を止めることがある。止められたら上の手順を実行してから終える。読み返して**追記が不要と判断したら、その理由を一言述べて終えてよい**(2回目は止められない)
- Claude Code の auto memory はマシンローカルで、セッション間・環境間・人の間で共有されない。**共有したい学びは必ずこのリポジトリに書く**

## 書き方の型

- **日付と出所を付ける**: `(2026-09-11・〇〇への返信の送信版で確定)` のように、いつ・何から分かったかを残す。後で矛盾したときに新しい方を選べる
- **差し戻しは対比で残す**: 「AIドラフト(直される側)| 送信版(正)」の2列。直された理由が1行で言えるならそれも書く
- **決定は表で残す**: 「論点 | 決定 | 理由」。理由がないと、後で同じ議論を蒸し返す
- **事実は言い切る。推測は「推測」と書く**。数字は出所を添える
- **記憶は少なく濃く**: 新しいファイルを作る前に既存ファイルへの追記を考える。1ファイル数ページまで。コアのファイルは20枚以内を目安にする
- **完了したこと・鮮度が切れたことは削除する**。Git履歴で戻せるので、アーカイブディレクトリは作らない。転用価値がある実績は `profile.md` の実績欄に移してから消す
- **機密は書かない**: パスワード・APIキー・個人の連絡先・第三者の個人情報はこのリポジトリに置かない(共有・AIへの投入を前提にした場所なので)

## 更新ルール

- `current.md` を更新したら、冒頭の「最終更新」日付を必ず更新する
- ディレクトリ名は英語(`people/` `projects/`)。ファイル名は日本語でよい(`people/山田製菓.md`)
- コミットメッセージは日本語でよい(例: `feedback.md お礼メールの締め方を追記`)
- このリポジトリはドキュメントのみなので PR レビューは不要。`main` に直接コミット・プッシュしてよい。作業ブランチが指定されている環境では、そのブランチにプッシュしたあと `main` へのマージ・プッシュまで自分で完了させる(ユーザーへの確認は不要)
- `CLAUDE.md` は `AGENTS.md` へのシンボリックリンク。編集は常に `AGENTS.md` に対して行う
- 相手別ファイルが増えたら `people/`、案件が増えたら `projects/` に置く。1相手=1ファイル、1案件=1ファイル
